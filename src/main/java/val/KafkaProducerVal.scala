package `val`

object KafkaProducerVal {
  val BOOTSTRAP_SERVERS = "bootstrap.servers"
  val ACKS = "acks"
  val RETRIES = "retries"
  val BATCH_SIZE = "batch.size"
  val LINGER_MS = "linger.ms"
  val KEY_SERIALIZER = "key.serializer"
  val VALUE_SERIALIZER = "value.serializer"
}
